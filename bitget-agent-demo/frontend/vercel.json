{
  "buildCommand": "npm run install:all && npm run build",
  "outputDirectory": "frontend/dist",
  "framework": null,
  "rewrites": [
    { "source": "/api/(.*)", "destination": "https://YOUR_BACKEND.onrender.com/api/$1" }
  ]
}
